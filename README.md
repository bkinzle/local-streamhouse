# Local Streamhouse
Creates local cloud native infrastructure for an Open Streamhouse, striving for parity with production.

## Quick Start
The following idempotent, all-in-one command will spin up the stack:
```shell
mise setup
```

When you're done, spin it down:
```shell
mise teardown
```
